name: freelance_phone_addon
description: Flutter addon package that mirrors the Taskup freelance React Native experience with gigs, projects, bids, disputes, and escrow flows.
version: 0.2.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^1.2.1
  intl: ^0.19.0
  equatable: ^2.0.5
  collection: ^1.18.0

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
